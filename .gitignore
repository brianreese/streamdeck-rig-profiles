# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage / test output
lib-cov
coverage
*.lcov
.nyc_output

# Dependency directories
node_modules/

# Build output
dist/
build/

# (data/ was previously used for local dev cache — govee cache now lives in
#  ~/Library/Application Support/com.rig.profiles/ — see src/setup.js)

# TypeScript cache
*.tsbuildinfo

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# Editor directories and files
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# macOS
.DS_Store

# Stream Deck CLI artifacts
*.sdPlugin/
scripts/scans/

# Your own profiles, not the project's. setup.js copies profiles.yaml.template
# here on first run, so a fresh checkout still works — while your family's
# names, colours and wheelbase slots stay out of the repo and out of any
# release built from it.
config/profiles.yaml

# Release staging and the packed plugin, rebuilt by docs/RELEASING.md.
/dist/
